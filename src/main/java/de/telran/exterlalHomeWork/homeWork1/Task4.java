package de.telran.exterlalHomeWork.homeWork1;

public class Task4 {
    public static void main(String[] args) {
        double radius = 7.5;
        double p = 2 * Math.PI * radius; // P = 2*pi*R
        double a = (Math.PI * radius) * radius;// Вычисляем площадь пиццы: s = PI * r^2

        System.out.println("Perimeter is = " + p);
        System.out.println("Area is = " + a);

    }
}
